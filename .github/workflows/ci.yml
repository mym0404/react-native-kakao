name: CI
on:
  push:
    branches:
      - main
      - v2
  pull_request:
    branches:
      - main
      - v2

permissions:
  contents: read

concurrency:
  group: ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
    outputs:
      android: ${{ steps.filter.outputs.android }}
      ios: ${{ steps.filter.outputs.ios }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Detect build changes
        id: filter
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
        with:
          script: |
            const { detectBuildChanges } = require('./script/ci-changes.js');
            await detectBuildChanges({ github, context, core });

      - name: 'Checked base: ${{ steps.filter.outputs.base }}'
        run: ':'

  lint:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup
        uses: ./.github/actions/setup

      - name: Build packages
        run: yarn build

      - name: Check
        run: |
          yarn t
          yarn test

#      - name: Cache Gradle
#        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
#        with:
#          path: |
#            ~/.gradle/wrapper
#            ~/.gradle/caches
#          key: ${{ runner.os }}-gradle-${{ hashFiles('example/android/gradle/wrapper/gradle-wrapper.properties') }}-${{ hashFiles('yarn.lock') }}-${{ hashFiles('**/package.json', '!node_modules/**') }}
#          restore-keys: |
#            ${{ runner.os }}-gradle-${{ hashFiles('yarn.lock') }}-${{ hashFiles('**/package.json', '!node_modules/**') }}-

#  test:
#    runs-on: ubuntu-latest
#    steps:
#      - name: Checkout
#        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
#
#      - name: Setup
#        uses: ./.github/actions/setup
#
#      - name: Run unit tests
#        run: yarn test
#
#  build-library:
#    runs-on: ubuntu-latest
#    steps:
#      - name: Checkout
#        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
#
#      - name: Setup
#        uses: ./.github/actions/setup
#
#      - name: Build package
#        run: yarn prepare

  build-android:
    needs: changes
    if: needs.changes.outputs.android == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup
        uses: ./.github/actions/setup

      - name: Build packages
        run: yarn build

      - name: Generate Android Example Project
        run: yarn gen:android

      - name: Install JDK
        uses: actions/setup-java@de7274f081f381c8f8158605e0321c36c376e2e6 # v6.0.1
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Finalize Android SDK
        run: |
          /bin/bash -c "yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses > /dev/null"

      - name: Cache Gradle
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.gradle/wrapper
            ~/.gradle/caches
          key: ${{ runner.os }}-gradle-${{ hashFiles('example/android/gradle/wrapper/gradle-wrapper.properties') }}-${{ hashFiles('yarn.lock') }}-${{ hashFiles('package.json', 'example/package.json', 'docs/package.json', 'packages/*/package.json') }}
          restore-keys: |
            ${{ runner.os }}-gradle-${{ hashFiles('yarn.lock') }}-${{ hashFiles('package.json', 'example/package.json', 'docs/package.json', 'packages/*/package.json') }}-

      - name: Restore Android E2E Debug app
        id: android-e2e-debug-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            example/android/app/build/outputs/apk/debug/app-debug.apk
            build/e2e/android/build.log
          key: ${{ runner.os }}-e2e-android-debug-x86_64-jdk17-v1-${{ hashFiles('mise.toml', 'yarn.lock', '.yarn/patches/**', 'package.json', 'packages/*/package.json', 'packages/*/src/spec/**', 'packages/*/android/**', 'example/package.json', 'example/app.json', 'example/react-native.config.js', 'example/android/app/**', 'example/android/build.gradle', 'example/android/gradle.properties', 'example/android/gradle/**', 'example/android/settings.gradle') }}

      - name: Build Debug app for E2E
        if: steps.android-e2e-debug-cache.outputs.cache-hit != 'true'
        env:
          JAVA_OPTS: '-XX:MaxHeapSize=6g'
        run: yarn e2e build android --abi x86_64

      - name: Save Android E2E Debug app
        if: steps.android-e2e-debug-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            example/android/app/build/outputs/apk/debug/app-debug.apk
            build/e2e/android/build.log
          key: ${{ steps.android-e2e-debug-cache.outputs.cache-primary-key }}

      - name: Start Metro for Android E2E
        run: |
          mkdir -p build/e2e/android
          yarn e2e metro android > build/e2e/android/metro.log 2>&1 &

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Verify example menus on Android
        uses: reactivecircus/android-emulator-runner@a421e43855164a8197daf9d8d40fe71c6996bb0d # v2.38.0
        with:
          api-level: '37.1'
          target: google_apis_playstore_ps16k
          arch: x86_64
          profile: medium_phone
          ram-size: 4096M
          disable-animations: true
          # API 37's gralloc mapper requires both features for DMA readback.
          emulator-options: -no-window -gpu software -feature GLDirectMem,HasSharedSlotsHostMemoryAllocator -noaudio -no-boot-anim -no-snapshot -camera-back none
          script: yarn e2e test android --device emulator-5554 --output build/e2e/android

      - name: Upload Android E2E evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: e2e-android
          path: build/e2e/android/
          retention-days: 14
          overwrite: true
          if-no-files-found: warn

  build-ios:
    needs: changes
    if: needs.changes.outputs.ios == 'true'
    runs-on: macos-latest
    timeout-minutes: 45
    steps:
      - uses: maxim-lobanov/setup-xcode@ed7a3b1fda3918c0306d1b724322adc0b8cc0a90 # v1.7.0
        with:
          xcode-version: '26.2'
      - name: Ensure iOS Simulator runtime
        run: |
          simulator_version=$(xcrun --sdk iphonesimulator --show-sdk-version)
          if ! xcrun simctl list runtimes | grep -Fq "iOS ${simulator_version}"; then
            xcodebuild -downloadPlatform iOS -buildVersion "${simulator_version}"
          fi
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Ruby
        uses: ruby/setup-ruby@14594264cd68ce8a2345dd349bc3d138a4ef85c8 # v1.327.0
        with:
          ruby-version: '3.1.2'

      - name: Setup
        uses: ./.github/actions/setup

      - name: Build packages
        run: yarn build

      - name: Generate iOS Example Project
        run: yarn gen:ios

      - name: Restore iOS E2E Debug app
        id: ios-e2e-debug-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            build/e2e/ios-build/Build/Products/Debug-iphonesimulator/KakaoExample.app
            build/e2e/ios/build.log
          key: ${{ runner.os }}-e2e-ios-debug-xcode26.2-v1-${{ hashFiles('mise.toml', 'yarn.lock', '.yarn/patches/**', 'package.json', 'packages/*/package.json', 'packages/*/src/spec/**', 'packages/*/ios/**', 'packages/*/*.podspec', 'example/package.json', 'example/app.json', 'example/react-native.config.js', 'example/Gemfile.lock', 'example/ios/**') }}

      - name: Cache cocoapods
        id: cocoapods-cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            **/ios/Pods
          key: ${{ runner.os }}-cocoapods-${{ hashFiles('example/ios/Podfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-cocoapods-

      - name: Install cocoapods
        run: yarn example pod

      - name: Build Debug app for E2E
        if: steps.ios-e2e-debug-cache.outputs.cache-hit != 'true'
        run: yarn e2e build ios

      - name: Save iOS E2E Debug app
        if: steps.ios-e2e-debug-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            build/e2e/ios-build/Build/Products/Debug-iphonesimulator/KakaoExample.app
            build/e2e/ios/build.log
          key: ${{ steps.ios-e2e-debug-cache.outputs.cache-primary-key }}

      - name: Start Metro for iOS E2E
        run: |
          mkdir -p build/e2e/ios
          yarn e2e metro ios > build/e2e/ios/metro.log 2>&1 &

      - name: Boot E2E Simulator
        run: |
          simulator_version=$(xcrun --sdk iphonesimulator --show-sdk-version)
          device_id=$(xcrun simctl create "Kakao E2E" "iPhone 17 Pro" "com.apple.CoreSimulator.SimRuntime.iOS-${simulator_version//./-}")
          echo "E2E_IOS_DEVICE=$device_id" >> "$GITHUB_ENV"
          xcrun simctl boot "$device_id"
          xcrun simctl bootstatus "$device_id" -b

      - name: Verify example menus on iOS
        run: yarn e2e test ios --device "$E2E_IOS_DEVICE" --output build/e2e/ios

      - name: Upload iOS E2E evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: e2e-ios
          path: |
            build/e2e/ios/
            !build/e2e/ios/device-state/daemon.json
          retention-days: 14
          overwrite: true
          if-no-files-found: warn

      - name: Stop E2E Simulator
        if: always() && env.E2E_IOS_DEVICE != ''
        run: xcrun simctl shutdown "$E2E_IOS_DEVICE"

  ci-result:
    needs: [changes, lint, build-android, build-ios]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Verify required CI results
        shell: bash
        env:
          CHANGES_RESULT: ${{ needs.changes.result }}
          LINT_RESULT: ${{ needs.lint.result }}
          ANDROID_CHANGED: ${{ needs.changes.outputs.android }}
          IOS_CHANGED: ${{ needs.changes.outputs.ios }}
          ANDROID_RESULT: ${{ needs.build-android.result }}
          IOS_RESULT: ${{ needs.build-ios.result }}
        run: |
          echo "changes=$CHANGES_RESULT lint=$LINT_RESULT"
          echo "android=$ANDROID_CHANGED:$ANDROID_RESULT ios=$IOS_CHANGED:$IOS_RESULT"
          for result in "$CHANGES_RESULT" "$LINT_RESULT"; do
            if [[ "$result" != 'success' ]]; then
              echo '::error::Change detection and lint must both succeed.'
              exit 1
            fi
          done
          for build in "$ANDROID_CHANGED:$ANDROID_RESULT" "$IOS_CHANGED:$IOS_RESULT"; do
            case "$build" in
              true:success|false:skipped) ;;
              *) echo '::error::A required build did not succeed or a build was not intentionally skipped.'; exit 1 ;;
            esac
          done

  e2e-report:
    needs: [changes, build-android, build-ios]
    if: always() && !cancelled() && (needs.changes.outputs.android == 'true' || needs.changes.outputs.ios == 'true') && github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: write
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Download E2E evidence
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: e2e-*
          path: e2e-results

      - name: Prepare E2E screenshots
        if: always()
        run: |
          for platform in android ios; do
            mkdir -p "e2e-images/e2e-$platform"
            cp e2e-results/e2e-$platform/*.png "e2e-images/e2e-$platform/" 2>/dev/null || true
          done

      - name: Publish E2E screenshots
        if: always()
        uses: JamesIves/github-pages-deploy-action@fa24774553152dd7873cd16ebd8d959b010c5445 # v4.9.0
        with:
          branch: gh-pages
          folder: e2e-images
          target-folder: e2e/pr-${{ github.event.pull_request.number }}
          clean: true
          commit-message: 'ci: publish E2E screenshots for PR #${{ github.event.pull_request.number }}'

      - name: Comment E2E results
        if: always()
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
        env:
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        with:
          script: |
            await require('./script/e2e-report.js')({ github, context, core });
